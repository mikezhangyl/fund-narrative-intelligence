# MIK-46 Acceptance CI Gate

Linear: MIK-46 `[ARCH-P1] Acceptance harness and CI gate for service contracts`

Scope:

- Make Stock Narrative Service acceptance deterministic and CI-runnable.
- Prove both service-first and local-fallback provider paths.
- Ensure conformance checks every endpoint declared in the contract YAML.
- Add the deterministic gate to GitHub Actions and document slice/release
  validation commands.

Verification is recorded in `verification.md`.
