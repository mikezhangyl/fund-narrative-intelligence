# Risk Notes

- The current export pack intentionally does not invent analyst notes; notes are accepted only through a local notes input artifact.
- Citation URLs may be empty when upstream source events expose metadata-only citations. The export still keeps source event IDs and source IDs.
- Notes are preserved as auditable user artifacts and cannot mutate or promote trusted state.
