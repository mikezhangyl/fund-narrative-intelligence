# Risk Notes

- Export package is JSON, not a compressed archive; future archive packaging can wrap the same manifest.
- Import overwrites the target workspace store only; it does not restore service-owned artifacts.
