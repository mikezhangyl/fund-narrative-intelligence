# Risk Notes

- Eastmoney diagnostics can still perform a live holdings fetch when `--provider-mode eastmoney` is used.
- Diagnostics intentionally does not write report artifacts; scripts that expect files should continue using the normal report command.
