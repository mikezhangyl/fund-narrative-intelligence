# MIK-41 Developer Handoff Format

Linear: MIK-41 `[P1][PM] Developer-ready implementation handoff format`

Scope:

- Add a tested developer-ready Linear issue handoff format.
- Document required sections, template, next-issue selection, and completion
  comment/checkpoint rules.
- Add startup memory pointer so future developer chats can discover the format.

Verification is recorded in `verification.md`.
